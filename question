1、将ocfs2文件系统中，某个文件的inode的签名写坏，文件系统会直接进入只读状态。
修改代码，让这种情况不返回只读，只返回eio，重新测试的现象是，文件系统没有进入只读状态，
并且被写坏的文件被修复到了其它的block上，这个修复的操作是哪个模块搞得？
并且，磁盘中还是留下了坏的inode block号

answer:看log，如果检测到坏的inode，原文件的inode会被删掉，重新打开文件时会重建？
那么会给这个文件重新创建一个inode，并且将数据也copy过去

2、对于extent的数据，也是同样的效果么？

3、如何创建数据存储十分离散的文件
